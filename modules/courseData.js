module.exports = function getData(param) {
    let data;
    if (param === 'PWA') {
        data = {
            lecturer: ["PPK", "Koop", "suus"]
        }
        return data;
    } else if (param === 'browser-technologies') {

    }
}
